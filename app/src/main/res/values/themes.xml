<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="nexora_night">#FF0B1220</color>

    <!-- Thème "coquille" pour l'Activity : l'UI réelle est en Compose.
         Fond bleu nuit dès la première frame (pas de flash blanc). -->
    <style name="Theme.NexoraTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/nexora_night</item>
        <item name="android:statusBarColor">@color/nexora_night</item>
        <item name="android:navigationBarColor">@color/nexora_night</item>
        <item name="android:colorBackground">@color/nexora_night</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.NexoraTV.Player" parent="Theme.NexoraTV">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
